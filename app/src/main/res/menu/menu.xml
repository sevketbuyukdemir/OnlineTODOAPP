<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/account_information_menu"
        android:title="@string/account_information_menu"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/add_post_menu"
        android:title="@string/add_post_menu"
        app:showAsAction="never"/>

    <item android:id="@+id/log_out_menu"
        android:title="@string/log_out_menu"
        app:showAsAction="never"/>
</menu>